import React from 'react';
import { Typography, Box } from "@mui/material";

const ROICalculator = () => {
  return (
    <Box sx={{ padding: 3 }}>
      <Typography variant="h2">
        KnoxBots ROI Calculator
      </Typography>
    </Box>
  );
};

export default ROICalculator;